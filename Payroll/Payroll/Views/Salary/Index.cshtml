@model IEnumerable<PayrollProject.Models.SalaryGrade>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Salary)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Grade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Salary)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Grade)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SalaryGradeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SalaryGradeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SalaryGradeID })
        </td>
    </tr>
}

</table>
